# 🎉 全局Toast提示使用指南

## 📍 显示位置

Toast提示现在统一显示在**屏幕正中间偏上方**（距顶部80px），而不是角落位置。

## 🎨 样式特点

- ✅ **渐变背景**：成功/错误/信息/警告四种类型
- ✅ **毛玻璃效果**：backdrop-filter模糊
- ✅ **优雅动画**：淡入淡出 + 轻微抖动
- ✅ **自动消失**：3秒后自动移除
- ✅ **多条堆叠**：支持多条消息同时显示

---

## 📖 使用方法

### **在管理后台组件中使用**

所有管理后台组件（AdminDashboard的子组件）都可以直接使用：

```javascript
// 成功提示
window.$toast('保存成功！', 'success')

// 错误提示
window.$toast('操作失败', 'error')

// 信息提示
window.$toast('这是一条信息', 'info')

// 警告提示
window.$toast('请注意！', 'warning')
```

### **在组件的showMessage函数中使用**

```javascript
function showMessage(msg, type = 'success') {
  if (window.$toast) {
    window.$toast(msg, type)
  }
  else {
    // 降级方案：如果Toast未加载，使用本地消息
    message.value = msg
    messageType.value = type
    setTimeout(() => {
      message.value = ''
    }, 3000)
  }
}
```

---

## 🎨 Toast类型

### 1. **成功 (success)** ✅
```javascript
window.$toast('保存成功！', 'success')
```
- 图标：✅
- 颜色：绿色渐变 (#10b981 → #059669)

### 2. **错误 (error)** ❌
```javascript
window.$toast('操作失败', 'error')
```
- 图标：❌
- 颜色：红色渐变 (#ef4444 → #dc2626)

### 3. **信息 (info)** ℹ️
```javascript
window.$toast('这是一条提示', 'info')
```
- 图标：ℹ️
- 颜色：蓝色渐变 (#3b82f6 → #2563eb)

### 4. **警告 (warning)** ⚠️
```javascript
window.$toast('请注意！', 'warning')
```
- 图标：⚠️
- 颜色：橙色渐变 (#f59e0b → #d97706)

---

## 📦 已应用的组件

以下组件已经使用全局Toast：

- ✅ **MarkdownEditor.vue** - 草稿箱编辑器
- ✅ **ArticleManager.vue** - 已上线文章管理
- ✅ **TrashManager.vue** - 垃圾箱管理
- ⏳ **MenuManager.vue** - 菜单管理（待应用）
- ⏳ **SiteConfigExtended.vue** - 网站配置（待应用）
- ⏳ **FetchArticleTool.vue** - 文章爬取（待应用）

---

## 🎯 位置示意图

```
┌─────────────────────────────────────────┐
│          顶部导航栏 (64px)               │
├─────────────────────────────────────────┤
│                                         │
│           空白 (16px)                   │
│                                         │
│    ┌───────────────────────────┐       │
│    │  ✅  保存成功！            │       │ ← Toast (屏幕中间偏上)
│    └───────────────────────────┘       │
│                                         │
│    ┌───────────────────────────┐       │
│    │  ❌  操作失败              │       │ ← 第二条Toast
│    └───────────────────────────┘       │
│                                         │
│         管理后台内容区域                 │
│                                         │
└─────────────────────────────────────────┘
```

---

## 🎬 动画效果

1. **出现动画**
   - 从上方淡入
   - 轻微缩放 (0.9 → 1.0)
   - 持续时间：300ms

2. **消失动画**
   - 向上淡出
   - 轻微缩放 (1.0 → 0.9)
   - 持续时间：300ms

3. **抖动效果**
   - 左右轻微摆动
   - 持续时间：500ms
   - 仅在出现时播放一次

---

## 📱 响应式设计

### **桌面端 (>768px)**
- 位置：顶部80px，水平居中
- 最小宽度：300px
- 最大宽度：600px

### **移动端 (≤768px)**
- 位置：顶部70px，左右各留16px边距
- 宽度：自适应容器宽度
- 字体略小：14px

---

## 🔧 技术实现

### **组件位置**
```
docs/.vitepress/theme/components/tools/GlobalToast.vue
```

### **挂载位置**
```vue
<!-- AdminDashboard.vue -->
<template>
  <div class="admin-dashboard">
    ...
    <GlobalToast />
  </div>
</template>
```

### **全局API**
```javascript
// onMounted时注册到window对象
window.$toast = addToast

// onUnmounted时清理
delete window.$toast
```

---

## ⚠️ 注意事项

1. **仅在管理后台可用**
   - Toast组件只在 AdminDashboard 中挂载
   - 仅管理后台的子组件可以使用

2. **降级方案**
   - 始终检查 `window.$toast` 是否存在
   - 如果不存在，使用本地消息组件

3. **消息时长**
   - 默认3秒自动消失
   - 不支持自定义时长（如需要可扩展）

4. **最大堆叠**
   - 没有限制，但建议不要同时显示超过3条
   - 过多消息会影响用户体验

---

## 🚀 示例代码

### **完整示例：保存操作**

```javascript
async function saveDraft() {
  try {
    const response = await fetch('/api/save', {
      method: 'POST',
      body: JSON.stringify(data)
    })
    
    if (response.ok) {
      window.$toast('✅ 草稿保存成功！', 'success')
    } else {
      window.$toast('❌ 保存失败', 'error')
    }
  } catch (error) {
    window.$toast(`❌ 保存失败: ${error.message}`, 'error')
  }
}
```

### **完整示例：表单验证**

```javascript
function validateForm() {
  if (!title.value) {
    window.$toast('请输入标题', 'warning')
    return false
  }
  
  if (!content.value) {
    window.$toast('内容不能为空', 'error')
    return false
  }
  
  window.$toast('验证通过！', 'success')
  return true
}
```

---

**🎉 现在所有Toast提示都会显示在屏幕正中间偏上方，体验更统一！**
